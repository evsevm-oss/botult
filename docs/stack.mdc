## Выбранный стек (этап 0)

- Язык/рантайм: Python 3.11+
- Telegram Bot API: `aiogram` (v3)
- Внутренние сервисы/REST: `FastAPI` + `uvicorn`
- Данные: PostgreSQL + SQLAlchemy (2.x) + Alembic (миграции)
- Кэш/сессии: Redis
- Очередь: Celery (брокер Redis)
- Объектное хранилище изображений: MinIO/S3-совместимые (через `boto3`/`minio`)
- HTTP-клиент: `httpx`
- Конфигурация: `pydantic-settings` + `.env`
- Логирование: `structlog`

### Контент для аватара/видео/голоса
- Image Gen: OpenAI Images (DALL·E) и/или Stability AI (SDXL)
- TTS: ElevenLabs (основной), OpenAI TTS (fallback)
- Talking‑Head: HeyGen или D‑ID (через их API)
- General Video (опционально): Runway/Pika/Luma — по потребности

